import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Lido Vault Solvency",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
